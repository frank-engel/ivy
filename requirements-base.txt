# Base requirements for IVyTools (headless API and batch processing)
# This file contains core dependencies needed for headless operation
# For GUI support, see requirements-gui.txt
# For development tools, see requirements-dev.txt

# Core scientific computing
numpy>=1.26.2,<1.27.0
pandas>=1.4.0
scipy>=1.10.0
matplotlib>=3.6.3

# Image processing (headless versions)
opencv-python-headless>=4.5.0
pillow>=12.0.0
imageio>=2.37.0
imutils>=0.5.4
scikit-image>=0.24.0

# Machine learning (for STIV)
numba>=0.58.1
joblib>=1.5.2
scikit-learn>=1.3.2

# Video processing (ffmpeg binary must be installed separately)
# On Linux: apt-get install ffmpeg libavcodec-extra
# On macOS: brew install ffmpeg
# On Windows: Download from https://ffmpeg.org/download.html

# Data formats
h5py>=3.10.0,<4.0

# Geospatial
shapely>=1.8.5,<3.0
utm>=0.8.0

# Network and utilities
requests>=2.32.0
python-dateutil>=2.9.0
tqdm>=4.67.1,<5.0
colormap>=1.1.0
tabulate>=0.9.0

# PDF generation (for reports)
reportlab>=4.1.0,<5.0

# Cross-section analysis (USGS internal package)
areacomp3 @ git+https://code.usgs.gov/RIVRS/areacomp.git@master

# Note: PyQt5 is NOT included here - it's only needed for GUI
# Install with: pip install image_velocimetry_tools[gui]
